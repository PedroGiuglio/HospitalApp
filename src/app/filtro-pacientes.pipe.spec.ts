import { FiltroPacientesPipe } from './filtro-pacientes.pipe';

describe('FiltroPacientesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPacientesPipe();
    expect(pipe).toBeTruthy();
  });
});
